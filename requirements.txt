Flask==3.0.3
numpy==2.1.3
matplotlib==3.9.2
pillow==11.0.0
# Physics engine for Phase-3 climber simulation. Pure-Python wrapper
# around Chipmunk2D. Chosen over pybox2d because pymunk's wheels are
# reliably built, the API is cleaner, and it integrates well with
# Gymnasium-based RL environments.
pymunk==7.0.1
# Gymnasium is the maintained successor to OpenAI Gym. The RL
# environment in `rl/` follows the standard reset()/step() API so
# Stable-Baselines3 / RLlib agents can be dropped in later.
gymnasium==1.0.0
