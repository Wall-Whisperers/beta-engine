"""Grid editor — Flask backend + static frontend for the wall editor.

Phase 1 component of the Beta Engine. Handles wall CRUD, validates the
JSON schema, and serves the vanilla-JS editor at `/`.
"""
